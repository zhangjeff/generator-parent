package ${package};

import ${baseModelPackage}.${domainName};
import ${baseModelPackage}.${domainName}Example;
import ${genericServicePackage};

public interface ${domainName}Service extends GenericService<${domainName}, ${domainName}Example, ${primaryKeyType}> {

}
